---
title: WHO 2022 ICD-11 Coding Allowability Policy
doc_type: policy
status: draft
owner: engineering
last_updated: 2026-09-24
---

# WHO 2022 ICD-11 Coding Allowability Policy

**Status: draft, owner review pending.** On 2026-09-24, with the owner's
approval, the draft was imported into the **dev database only** (through
`flask icd11 policy-import`; no row marked `reviewed`) so the owner can review
it in the admin ICD-11 browser. No migration reads it, and no other database
has it.

## Purpose

This policy defines which ICD-11 MMS 2026-01 categories (`mas_icd11_mms`) are
selectable during WHO 2022 verbal autopsy ICD coding, and their sex and age
restrictions. It is the ICD-11 counterpart of
[WHO 2022 ICD-10 Coding Allowability](who-2022-icd10-coding-allowability.md)
and resolves decision D3 of
`docs/planning/icd11-coding-screen-integration-plan.md`.

Until a policy is imported, every ICD-11 category is
`is_coding_selectable = NULL`, so ICD-11 coding is impossible.

Selectability is independent of COD bucket mappings (owner decision
2026-09-21). Like the ICD-10 policy, it is drafted from WHO's annex (owner
decision 2026-09-24).

## Artifact

Generated by `flask icd11 policy-draft`
(`app/services/icd11_policy_draft_service.py`) into
`docs/icd-causegrp-mappings/migration-artifacts/who-2022-icd11-policy-draft-2026-09-24/`:

- `who_2022_icd11_mms_2026_01_policy_draft.json`: the policy, in the format
  `flask icd11 policy-import` and the admin ICD-11 browser import read
  (`import_icd11_mms_policy_json`).
- `icd11_policy_review.csv`: every category outside chapter X, selectable or
  not, with the rule that decided it, its sex/age, where they came from, and
  review flags.
- `README.md`: counts per chapter, per rule, per restriction and per flag.

The JSON is a **full replacement**, as the ICD-10 one is: the importer sets
every active category it does not list (chapter X included) to not selectable
with no sex, age or note. Items carry no `policy_status`, so an import leaves
each row's status unchanged and a regenerated draft never clears a `reviewed`
mark. A restricted row's `restriction_note` records where its restriction came
from (`Draft: ...`).

## Selectable categories

1. **Annex.** Every catalogue category covered by an ICD-11 range of WHO's
   annex (`docs/icd-causegrp-mappings/ICD-to-VA-Buckets/who_2022_va_cause_list_icd10_icd11.csv`),
   expanded with the bucket generator's `parse_icd11_ranges` and
   `expand_range`: the codes lexically between the range's ends plus their
   descendants. The malformed `5C52.Y-5C52-Z` is read as `5C52.Y-5C52.Z`
   (owner decision 4). The other annex range oddities are kept as expanded;
   codes a range reaches past its written end are flagged
   `past_written_end`.
2. **Owner decision 5a** (`docs/policy/icd10-to-icd11-transition.md`
   section 6): `5A20`-`5A2Y`, `3A50`, `3A4Z`, `DB94`, `1C8G`, `1C8H` and
   `RA01`, each with its descendants.
3. **Never selectable**, whatever rules 1-2 say: chapter 24 (Q, factors
   influencing health status), chapter 26 (S, traditional medicine), chapter
   V (functioning) and chapter X (extension codes); and every chapter 25 (RA)
   emergency code outside the `RA01` family.
4. Everything else is not selectable.

**Parents and residuals.** A parent category and its residual
(`.Y`/`.Z`, `Y`/`Z`) categories are selectable when a range covers them. This
follows the ICD-10 policy, which selects codes at whatever level WHO wrote
them: 779 of its selectable dotted codes sit under a selectable
three-character parent, and it never excludes residual `.8`/`.9` codes.

One difference to note for review: ICD-10 ranges written at three-character
level select only three-character rows, while ICD-11 ranges cover every
descendant. The draft follows the brief (every covered category), so it
selects far more ICD-11 categories than ICD-10 rows (see the artifact README).

## Sex and age

Allowed values and matching are the ICD-10 policy's (`both | female | male`;
`all | neonate | infant | child | adult`, exact-or-all). Default: both sexes,
all ages. For a selectable category, the first rule that applies wins:

1. **Chapter rules**, the ICD-10 policy's blanket chapter rules on the
   equivalent ICD-11 chapters:
   - chapter 18 (`JA`-`JB`, pregnancy): female, adult, as ICD-10 `O00`-`O99`
   - chapter 19 (`KA`-`KD`, perinatal): neonate, as ICD-10 `P00`-`P96`

   Chapter 20 (`LA`-`LD`, developmental anomalies) has **no chapter rule: all
   ages** (owner, 2026-09-24), although ICD-10 makes `Q00`-`Q99`
   neonate-only. Its categories take sex (and any code-level age) only from
   the rules below.
2. **Block rules**, the ICD-10 policy's sex-specific neoplasm ranges on their
   ICD-11 blocks, with descendants: `2C70`-`2C7Z` female (`C51`-`C58`),
   `2C80`-`2C8Z` male (`C60`-`C63`), `2F31`-`2F33` female (`D26`-`D28`),
   `2F34` male (`D29`). WHO's map sends `C53` to `2C77.Z` only, so without
   these the parent `2C77` and its specific children would stay both sexes.
3. **ICD-10 carry-over.** WHO's `10To11MapToOneCategory` (2025-01; targets
   translated to 2026-01 through the `MovedTo` rows of the 2026-01 change
   list, as the bucket generator does) gives each ICD-10 code one ICD-11
   target. The ICD-10 restriction is the code's row in the reviewed ICD-10
   policy (`who-2022-va-icd-cod-2026-revision/who_2022_icd10_2019_2_policy_reviewed.json`),
   or for a dotted code not in that policy, its three-character parent's row.
   **ICD-10 `O`, `P` and `Q` codes are never carried** (owner, 2026-09-24):
   their restrictions are blanket chapter rules, not judgements about the
   condition, so they must not follow a code into any ICD-11 chapter (e.g.
   `GB81` autosomal dominant polycystic kidney disease stays both/all despite
   `Q61.2`). Chapters 18 and 19 get their restriction from rule 1 instead.
   Genuine code-level carries stay, such as `A33` to `1C15` (neonate) and
   `R95` to `MH11.x` (infant).
   A category takes the restriction when every ICD-10 code mapped to it
   agrees. When they disagree it stays both/all and is flagged
   `icd10_conflict`; when its only restricted sources reach it through a
   postcoordinated (`&`) or alternative (`/`) target it stays both/all and is
   flagged `icd10_not_one_to_one`.
4. **Inherited** from the parent's carried ICD-10 restriction.
5. **From children**: a category left at the default takes the restriction
   all its selectable children share (e.g. `MH11` from `R95`'s infant rule on
   `MH11.0`/`.1`/`.Z`).

A chapter or block rule that replaces a different carried restriction is
flagged `rule_overrides_icd10`. A parent whose children end up with different
restrictions is flagged `children_differ`.

ICD-10 `restriction_note` values are not carried: none of the reviewed ICD-10
rows has one.

## Review

The owner reviews the draft in the admin ICD-11 browser
(`/admin/panels/icd11-browser`, dev database) or through the review CSV,
starting with the flagged rows. To take it back out of dev before sign-off,
set the five policy fields of release `2026-01` back to their pre-import
values (every category was `is_coding_selectable`, `sex_selectable`,
`age_group_selectable`, `restriction_note` = NULL, `policy_status` =
`unreviewed`; the import did not touch `policy_status`). Applying it to
deployments is a separate step: a data migration that reads the frozen JSON,
after the owner signs off.
